package commandhelper;

import elements.Movie;
import elements.MovieCollection;

public class MovieAdder {
    public void add(MovieCollection collection, Movie movie) {
        collection.addMovie(movie);
    }
}
